{
  "name": "ezra-todo-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "End-to-end integration tests (UI -> API -> DB) for ezra-todo",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
